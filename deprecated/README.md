# Deprecated Code

Files moved here are from earlier development phases. They worked at the time but have been superseded by the current USF pipeline (`src/gt2_to_usf.py → src/usf_to_sid.py`).

Each subdirectory has its own README explaining what the code did and why it was deprecated.
