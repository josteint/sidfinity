### C19 — Hand-patched player wedge (SMC opcode toggle / skipped-load variants)
- **Canonical:** a scene-circle "editor mod" ships a CANON player with a few
  BYTE-LEVEL WEDGES (a JMP over 2-3 canonical loads, a JSR re-pointed through
  a stub, a store retargeted at another instruction's OPCODE = an SMC toggle).
  Diagnose with the divergence recipe, then: (1) dump the ORIG's bytes at the
  canon site the disassembly says produces the diverging write — a wedge shows
  as a changed opcode (`JMP` where canon has `LDA abs,y`); (2) fingerprint the
  wedge bytes base-relative and CENSUS the whole family for carriers (both to
  size the class and to prove 0-FULL regression exposure); (3) reproduce the
  wedge's SEMANTICS in the composer behind a factory-probed boolean param —
  e.g. `hardrestart_smc_variant`: PW step base never loaded + phase/dir persist + the
  hard-restart TEST write gated by a global flag toggled per note-init from
  the instrument's $04 flag (the orig toggles the STA/LDA opcode at $17FB;
  the composer keeps an explicit `hrtest` byte primed from the file-image
  opcode). Reproduce state-machine EFFECTS, never the SMC mechanism.
- **Trap:** the file-image state of the toggled byte differs per member (save
  moment) — it is PRIMING, so capture it as a param (`hardrestart_test_init`), don't
  assume a constant.
- **Diagnosis tell:** runtime state ≠ file-image table byte while taint_source
  says the byte is STATIC ⇒ the READ SITE differs from canon — dump the
  operand/opcode at the canon site.
- **8th occurrence (round 60, 2026-07-09):** the PW-DIRECTION reset redirect
  (Artlace/End_of_1992_intro + The_Syndrom/Black_It, only carriers in f1):
  the note-init pulse reset's second store `LDA #$00 / STA phase,x /
  STA dir,x` has its direction operand re-pointed at an INERT byte (Artlace:
  the unused $179E-$17AF state gap; Black_It: the post-note guard, which
  note-init unconditionally overwrites to 2 right after) → the PWM sweep
  DIRECTION persists across note-inits while value/bounds/step/phase still
  reset. Tell: at a note-init both streams write the same fresh PW value,
  next frame orig sweeps DOWN (continuing the pre-note direction) where the
  rebuild sweeps UP. `factory._pulsewidth_dir_persist_probe` (anchor `A9 00 9D
  <base+$762> 9D <op>`, reloc-aware, positive minority: op != base+$765) →
  `pulsewidth_dir_persist` param → composer drops the one `sta pwdir,x` line from
  the pulse-reset block.
- **9th occurrence (round 63, 2026-07-09) — INIT-PREFIX subtune force:** the
  wedge is a 2-byte init WRAPPER, not a body patch. Sans_intro's PSID init =
  `$0FFE` (base $1000 − 2): `A9 01` (LDA #$01) falling straight through into
  the canon `$1000: 4C 1D 10` (JMP $101D = tune-select, `A*8→Y`). So EVERY
  play hard-forces tune record 1 regardless of the PSID song number — but the
  extract's `for sub in range(n_subtunes): rec = tunetab + sub*8` walked record
  0, a DUMMY record whose V1/V2 tracks are `$FE` (immediate stop), so V1/V2
  were dropped and the rebuild played the primed idle note under an empty
  orderlist (`orderlist: stop`). This is a DERIVATION wedge (changes WHICH
  record is musical content, not a value), so EXTRACT-ONLY: `_forced_subtune_probe`
  (init≠base + `mem[init]==$A9` + `base` is the canon `JMP base+$1D` dispatch +
  the `LDA #imm` reaches it by fall-through or `JMP base`) → `DMCV4Config.
  forced_subtune` → `extract` walks `rec = tunetab + forced*8`. NO USF field,
  NO composer change (the composer plays the walked content; the forced index
  is an engine artifact per the principle §8). Ground truth: memwatch $1707/
  $170A (runtime track ptr lo/hi) = $1A36 = record 1; pc-trace `$101d f 01`
  (A=1) + `$180d ... 1ac6,Y [1ace]` (Y=8) confirm the force. REGRESSION-SAFE
  BY CONSTRUCTION: `forced` is None for canon init==base (byte-identical) and
  imm==0 reproduces the record-0 walk; the dispatch guard rejects banking /
  other LDA#-leading wrappers. Census over 5833 f1: exactly 2 carriers, both
  previously partial (Sans_intro fall-through form, Devilock/Sub_Effect
  JMP-to-base form) ⇒ 0 FULL exposure. Sans_intro FULL 255559/255559 state ✓;
  full `tools/regression.py` green (0 regr all 8 families). TELL: a rebuild
  that plays a voice's PRIMED IDLE NOTE under an empty orderlist while orig
  plays a full part on that voice = a wrong-tune-record walk — memwatch the
  runtime track-ptr ($1707/$170A) + pc-trace the init A at the tune-select.
  **3rd FORM (round 138, 2026-07-30, Odysseus/Hear_Circa_2_Minutes, imm=3
  → FULL 271977):** the forcing `LDA #imm` sits DEEPER in a longer init
  wrapper and reaches init via **`JSR base`** (`A9 imm 20 <base>`; the
  wrapper continues with CIA latch programming after the JSR). Probe scans
  the wrapper window `[init, min(init+$30, base))` for the 5-byte pattern —
  the exact 16-bit JSR target adjacent to an LDA# is the static anchor.
  TELL variant here: the rebuild played a COHERENT but entirely DIFFERENT
  song (record 0 is a full other tune, not a dummy) — same class, richer
  disguise. JSR-shape family census: 1 behavior-changing carrier + 8
  imm=0 no-ops (7 Rayden 2SIDs + Praiser/Mega_Mix).
  ⚠ REFINED r139 (2026-07-30): the static JSR-shape match alone MIS-FIRED
  on a CONDITIONAL wrapper (Bomberman_preview: `CMP #$00 / BNE / LDA #$05
  / JSR base` remaps ONLY subtune 0 to song 5 — a byte scan cannot see
  the branch; the r138 census missed it via an extra `B > a` guard the
  probe lacked). The 3rd form is now OBSERVATION-CROSS-CHECKED
  (`_init_song_observe`): fire only when py65-run init(A=sub) enters base
  with A == imm for EVERY header subtune. Bomberman correctly refused
  (stays at its pre-existing partial baseline); its per-subtune
  conditional song remap is a recorded future lead.
  **→ RESOLVED as the 38th occurrence (round 159, 2026-08-01,
  Bomberman_preview → all 4 subtunes FULL, sub 0 161157/161157 state ✓):**
  the CONDITIONAL wrapper needs a PER-SUBTUNE map, not a uniform force.
  New `_subtune_song_map_probe` (same static-anchor gate as the 3rd form,
  so the py65 observation stays off the hot path) runs `_init_song_observe`
  and returns the observed A-per-subtune list iff it is NON-identity AND
  NON-uniform (identity = default walk; uniform = `forced_subtune`). The
  standard tune-select dispatch does `A*8 → Y`, so the observed A IS the
  record index. Carried by `DMCV4Config.subtune_songs` (list); `extract`
  generalizes `forced` int→list via `_rec_of(sub, forced)` at the four walk
  sites (off-image sectors / secp reads / track ptrs / decode). Ground truth:
  `siddump --reinit-snapshot $1085 $1707-$170C` = track ptrs $1B14/$1B34/$1B54
  = **record 5** (`tunetab+5*8`), vs the extract's record-0 $1ADD/$1ADF/$1AE0.
  REGRESSION-SAFE: `_rec_of` is identical to the old `(sub if forced is None
  else forced)` for None/int; the probe returns None for identity/uniform/
  no-wrapper. Census: exactly 1 anchored member in the DMC corpus = the sole
  carrier, 0 perf/regression exposure. Relation: forced_subtune's conditional
  sibling; OBSERVATION-based like C18/C31 (the subtune→song dispatch family).
- **30th occurrence (round 140, 2026-07-30) — PLAY-CLOCK COUNTER BYTE INSIDE
  A PLAYED SECTOR (the Ed-animator family's sharpest form; PVCF/Dresden_Party
  FULL 172817):** the appended play wrapper (`INC addr / LDA addr / AND #$01 /
  CMP #$01 / BEQ fx / JMP base+3`, init seeding `LDX #$FF / STX addr`) keeps
  its phase-parity counter at $6FFF — INSIDE V3's pattern data. A mode-0
  glide row's START-NOTE byte sits AT that address, so the row plays the
  LIVE counter (curnote = play index & $FF — the pitch rises one step per
  play tick each pattern lap; the stale file byte decodes as a plausible
  ordinary glide, which is the disguise). TELL: first divergence is a
  note-init freq LO only (the freq-HI coincides — orig reads its off-table
  stable-prefix vol slot, the rebuild a real table byte). Fix: static probe
  `_playclk_probe` (INC/LDA operand pair at the play vector + the init
  seed) → `extra_params.playclk_addr` (extract-INTERNAL — filtered from the
  USF params; the address is engine-positional); the WALK flags any glide/
  note row whose source byte sits at the addr (USF row flag `note_clock` —
  grammar + parser extended, corpus check 12001/12001); the COMPOSER
  reproduces the mechanism exactly: the flagged row's stream byte is
  emitted as a labeled `$FF` seed, `playclk:` INCs every labeled byte at
  the head of EVERY play call (before phase dispatch), init re-seeds $FF.
  Off-table freq for the counter notes needed NOTHING new — the composer's
  flo/fhi adjacency + co-located window matched the orig's layout. Census:
  6 wrapper-shape carriers; only Dresden's counter lands in song data (the
  rest count in a wrapper-local byte below base → no rows flagged, byte-
  identical; Dresden_Party_95_II + 2_Speed re-verified FULL).
- **31st occurrence (round 142, 2026-07-30) — pulse-step BASE read re-pointed
  into SID-MIRROR space (PVCF/Mathematica_tune_3 FULL 210182):** canon $1376
  `ADC $175F,X` (pulse base nibble) patched to `ADC $D75F,X` — X=0 reads the
  live ENV3 register, X=1/2 read write-only SID mirrors (the decayed data
  bus). The STORE to $175F was NOT re-pointed, so off-table windows still
  co-locate cpwbase correctly; only the two composer ADC sites (fx_pulse +
  the R-phase pulse_tail) swap to the absolute read. TELL: a note-init PW-lo
  off by exactly the instrument's base nibble (ours added the record nibble,
  orig added the ~0 bus read). `_pw_base_read_probe` (canon-site operand,
  first row in the uniform _WEDGE_PROBES table) fires ONLY for operands in
  $D400-$D7FC — hardware-stable and layout-independent, so the composer
  reproduces the EXACT absolute read and gets identical values under the
  identical preceding write stream; a repoint at a RAM var would NOT be
  verbatim-reproducible and keeps the default (honest residue). The member
  also carries an init wedge (`LDA #$35 / STA $01` banking poke + forced
  A=0 — a no-op for the walk). Census: exactly 1 carrier in 10,689.
- **10th occurrence (round 65, 2026-07-09) — $D418 RE-ASSERTED EVERY FRAME
  (COMPOSER param, not extract-only; also C10 master-vol-every-frame form):**
  Groove/Bakewell_Dwayne (+ Rap/Hands_up_Ravers, Rorschach/For_Vandalism_27 —
  the only 3 f1 carriers). The wedge changes a write-stream TIMING, not a
  derived value, so it needs a composer param. The play-body global filter
  routine's `STA $D417` is replaced by `JSR <wrapper>`, and the wrapper does
  `STA $D417 / LDA #mode / ORA mvol / STA $D418 / RTS` → `$D418 = mode | mvol`
  is re-written EVERY frame (at the filter-tail END); the canon filter note-init
  `STA $D418` ($12A8) is neutered to `BIT $D418` and its preceding `STA $2004`
  self-modifies the wrapper's mode immediate per note-init. `factory.
  _master_vol_reassert_filter_tail_probe` → USF param `master_vol_reassert_filter_tail` (initial mode imm) →
  composer: note-init stores `fdmode` to a `d418mode` shadow (suppress the
  note-init `$D418`), the per-frame filter tail re-asserts `lda d418mode / ora
  mvol / sta $d418`, init primes `d418mode`. Sibling of `master_vol_every_play` (the
  play-VECTOR wrapper form, `$D418` at play START); this is the filter-tail END
  form. Default None → byte-identical. **PROBE-ANCHORING LESSON (why the STATIC
  opcode probe must target the REACHABLE site, not a byte pattern):** the first
  LOOSE probe (`STA $D417 .. STA $D418` anywhere) false-fired on Qbhead_01's
  aux/init routine (`STA $D416 / LDA #imm / ... / STA $D418` at $1CA8) whose
  live per-frame routine is canonical — it would have REGRESSED a FULL member.
  Anchoring on the play-body computation shape (`STA $D416 / LDA abs / ORA abs /
  JSR-wrapper` at +9) excluded it. Caught by localizing each census carrier's
  first divergence BEFORE committing (the orig had no per-frame `$D418`). f1 3
  carriers all previously partial ⟹ 0 FULL exposure; Groove FULL 155620/155620;
  full regression green (0 regr all 7 families).
- **11th occurrence (round 70, 2026-07-10) — SWITCH ($7D) GATE-MASK TOGGLE
  IMMEDIATE (COMPOSER param):** Bax/Feed_a_Bird (the ONLY carrier in all 5833
  f1 members). The tie/legato handler at base+$183 canonically toggles ONLY the
  gate bit: `LDA gatemask,x / EOR #$01 / STA gatemask,x` (mask $FF<->$FE = gate
  as the wave table says <-> force gate off). The wedge patches the EOR
  immediate $01->$1F (byte at base+$18D), so a SWITCH toggles
  gate+test+ring+sync+triangle ($FF<->$E0) — CUTTING a triangle/ring/sync note
  to SILENCE ($17 & $E0 = $00) where canon merely releases the gate
  ($17 & $FE = $16). Presents as V3 ctrl orig $00 vs rebuild $16 at a legato
  boundary (localize the flat write-stream, then memwatch the gate mask $100f+v
  across the transition — it goes $FF->$E0, NOT ->$FE; the pc-trace shows
  $118C = `49 1F` not `49 01`). MISSED by `dmc_canon_diff` — an immediate-value
  tweak (unchanged opcode $49, no operand repoint) sits in its documented blind
  spot. Fix: `factory._switch_toggle_mask_probe` (STATIC opcode probe, anchors
  on the LDA/STA operands = gatemask_addr, reloc-aware, guards gatemask_addr
  None) → new USF param `switch_toggle_mask` (the toggled bit-set; default $01)
  → composer's `ev_switch` emits `eor #<mask>`. Default $01 -> byte-identical
  text. REGRESSION-SAFE BY CONSTRUCTION: the composer applies the probed mask
  verbatim so its $D404 write can only match the orig MORE often, never less;
  and $E0 vs $FE coincide for noise/pulse/saw notes (only bits 5-7 survive
  either mask), so the value only bites on sync/ring/test/triangle notes.
  Census 5833 f1: 1 carrier (partial), 5502 canon $01, 0 FULL exposure.
  Feed_a_Bird partial -> FULL 130578/130578 state ✓; full regression green
  (0 regr all 8 families: Hubbard 71, Companion 44, C64ME 15, Jay_Derrett 17,
  FC 31, DMC 12, Basic 22).
- **12th occurrence (2026-07-21) — the MULTI-SID RELOCATION MISS (COMPOSER
  param):** not an editor's hand patch but the same shape from the 2SID
  relocation tool. The editor builds chip 2/3's player by copying chip 1's and
  adding the chip offset to every `$D4xx` store operand; some builds MISS one,
  so that write lands on chip 1 for both players. `composer_asm._reloc_sid_regs`
  had generalised the ONE observed carrier (Surgeon/Nice_Dream's res/route
  `$D417`) into a hardcoded `keep_res=True` default — which then omitted chip
  2's res/route write for every correctly-relocated member (Rayden's 7 2SID
  builds: first divergence at flat position 21, orig `$D437=$F1`, rebuild
  nothing). Fix = the canonical form: STATIC operand probe
  (`factory._multisid_keep_regs`, classifies each `$8D/$99/$9D` store as
  own-chip vs chip-1 using the header's real chip address) → per-chip
  `multisid_keep_regs` param → `_reloc_sid_regs(keep_regs=...)`, default EMPTY
  (fully relocated). **LESSON — a probe generalised from one carrier is a
  hardcoded default in disguise:** `keep_res=True` read as a documented quirk
  but was an un-probed assumption, and it was wrong for 7 of the 8 detected
  carriers. **GRANULARITY TRAP:** the miss is per-STORE, not per-register —
  Nice_Dream also has 1 of its 3 `$D401` stores un-relocated. A register whose
  stores are MIXED is not representable at register granularity, so the probe
  returns only registers ALL of whose stores are un-relocated (mixed ones are
  relocated, matching pre-probe behaviour); closing that needs role-tagged
  emitter sites. A SECOND carrier of the mixed case surfaced 2026-07-21 —
  Surgeon/Cow_Anus_Fucked's chip-2 player leaves the sidwrite freq-lo tail
  (`STA $D400,y` at base+$60D) un-relocated among three relocated `$D400`
  stores, so ALL THREE of chip 2's voices write freq-lo onto chip 1. It
  presents as one write appearing on the wrong CHIP in an otherwise aligned
  frame (localize per chip, C28). **CLOSED 2026-07-21 — the role-tagged
  emitter sites, built:** a `keep_regs` entry gains an optional `@label`
  form (`00@sidwrite` beside the whole-register `17`), and the relocator
  scopes it to the lines after that composer label and before the next one
  (the composer emits one label per routine, so a label IS the role). The
  store is named by WHAT ITS BLOCK DOES, never by an address — the composer
  re-architects the player, so only the role survives. A canon-site ->
  role table (`_SIDSTORE_ROLE`) covers the sites we can name confidently;
  an unmapped site keeps the old behaviour (relocate the whole register)
  rather than guessing. Extend it a row at a time. Cow_Anus_Fucked partial
  -> FULL (129731/129731); Nice_Dream probes `17,01@cymburst` (its
  noise-attack burst relocates freq-lo but not freq-hi) and is unchanged,
  blocked by an unrelated single-chip drift. Census over all 10,676 DMC members: 19 multi-SID headers, 8
  detected, 7 fully-relocated + 1 keep=`$17` ⇒ 0 FULL exposure. NB the chip's
  real I/O address is used only to CLASSIFY operands — the composer still
  standardises chip k to `$D400+k*$20` (C27) and the verdict is chip-tagged,
  so a member whose chip 2 sits at `$D500` (Rayden) needs nothing carried.
  Occurrences live in [[project_dmc]].
- **14th occurrence (round 95, 2026-07-23) — GLIDE-SPEED STORE RE-POINTED
  INTO SONG DATA (glide dead + a runtime DATA POKE):** Bleed_Into_One/
  Ice_on_Fire patches the canon glide/slide dispatch's speed store
  (`$1136: STA $1741,X` → `STA $1F41,X`). TWO effects, both needed for
  FULL: (1) `glsp` is never written, so NO glide/slide ever moves — every
  `$Cx` plays note A verbatim (musically the engine's speed-0
  glide-cancel; the extract forces the decoded speed nibble to 0,
  `_SecFmt.glide_dead`, byte consumption + target unchanged, composer
  untouched); (2) `$1F41` sits INSIDE the song data (sector 20, note
  byte at pos 37), so each voice's executed speed nibble is POKED over
  the byte at `target+X` — runtime-generated musical content (V1's `$C4`
  rows poke `$04`; V3 later audibly plays note 4 there, whose wave
  offset −12 then reads freq[248] = live durrel state — the value the
  divergence surfaced). The extract simulates the poke on a per-song
  memory copy when the poking voice's speed nibbles are a SINGLETON
  (`_glide_poke_overlay`); multi-speed voices have no static effective
  byte and are left to the verdict. PROBE (three cuts, two rejected):
  compare the dispatch STORE operand against the fx_glide READ operand
  (the runtime consumer), BOTH anchored at fixed canon offsets from the
  member's base (store base+$131, read base+$41C) and the read required
  canon-consistent (== base+$741); fail-open otherwise. Rejected cut 1:
  `store == gla_store − 3` flagged Rocket_n_Roll (FULL; player at $5000,
  store/read correctly $5741, gla operand a STALE dead-path canon $1744 —
  the r93 partial-relocation pattern). Rejected cut 2: whole-image regex
  pairing mixed sites from different packed players in compilations.
  Census: probe fires on ~16 members; all 11 baseline-FULL carriers
  re-verified FULL (firing is harmless without sounding glides); the
  diagnosing DIVERGENCE TELL was "our note exactly 2 below orig's at a
  note-init, second pattern occurrence only" — the first occurrence
  matched because the poke hadn't landed yet on the first read.
  Occurrences live in [[project_dmc]].
- **15th occurrence (round 96, 2026-07-23) — TRACK-LOOP IMMEDIATE patched
  in the CANON $FF handler (loop-to-N):** Brian/They_Are_the_Best_1 patches
  the in-player loop code's immediate (`C9 FF D0 08 A9 00 9D otrk,x` →
  `A9 02`), so every voice's `$FF` loops to track position N instead of 0 —
  the same SEMANTICS as the C13 reset-all-to-N SYNC hook, expressed as a
  1-byte immediate tweak that (a) the canon-shaped `loop_site` detection
  sails past (the site matches, only the immediate differs) and (b)
  `dmc_canon_diff` documents it cannot see (its stated immediate-tweak
  LIMIT). PRESENTS: at the wrap, both sides note-init at the same instant
  with a completely different ROW (note+instrument+PW all differ) and the
  orig's post-wrap transpose is the pre-wrap value (the loop lands PAST the
  track's leading transpose command); plus a small length tail. FIX: a
  static probe in `dataflow.py` (signature + STA operand must equal the
  fetch-anchored track-position address) sets the existing
  `loop_reset_pos = N` — no extract/composer change. Census: 12 carriers
  corpus-wide, 0 baseline-FULL (no exposure); the one probe landed **7
  members FULL** (Brian + 4 Cubehead + Martens/4_Simone +
  The_Syndrom/Quattrodance); Just_11/Witchs_Birthday/Conversion carry
  other blockers, Arthur's 2 are `nonstandard_vectors` builds. Occurrences
  live in [[project_dmc]].
- **16th occurrence (round 101, 2026-07-23) — non-filter route-bit CLEAR
  re-pointed off the $D417 shadow:** Daf/Classic_Mix re-points ONLY the
  note-init clear's store (canon $12C6 `STA $1018` → `STA $101C`, a void
  byte) while the filter-instrument SET site ($1276) still targets the
  shadow — so $D417's routing bits ACCUMULATE and never clear; the
  work-file leftover ($07, already carried as `init.sid.filter.
  res_routing`) persists although voices keep note-initing non-filter
  instruments. PRESENTS: a frame-1 `$D417` divergence (orig $07 vs
  rebuild $07&~voicebit) right after the first voice's note-init of a
  non-filter instrument. FIX: `_route_clear_dead_probe` (anchors BOTH
  sites' full canon shape, fires iff set-store==shadow ∧ clear-store≠
  shadow, fail-open) → `extra_params['route_clear_dead']` → the composer
  emits NO clear in `ni_filter` (gated f-string block `route_clear`;
  everyone else byte-identical). This is a COMPOSER-param wedge (changes
  a write-stream VALUE). Census + occurrences in [[project_dmc]].
- **17th occurrence (round 102, 2026-07-23) — rest-tail RTS = rest_effects
  'none' (a FOURTH fetch-frame variant):** Doxx/Bassy_Introtune patches the
  rest/switch/slide tail's dispatch (canon $1180 `JMP $1322` run-effects)
  to a plain **RTS** — a voice fetching a rest row runs NO effects that
  frame, not even the wave-step SID refresh (the existing family-2 'skip'
  still refreshes). PRESENTS: a play where the resting voices emit ZERO
  writes (e.g. V1 hard-restart + filter tail only) while the rebuild
  emits their full refreshes — first visible at the song's first rest-row
  fetch; beware siddump 0-play buckets + straddled plays mimicking short
  plays (flat stream disambiguates). FIX: extend BOTH rest-dispatch
  probes (canon-path `$1180` opcode check + the layout-independent
  `_dataflow_knob_probes` shape scan) with the RTS case →
  `rest_effects='none'` → composer `rest_none: rts` target (label + the
  extended per-subtune dispatcher arm gated on actual use — everyone
  else byte-identical). Census + occurrences in [[project_dmc]].
- **18th occurrence (round 106, 2026-07-23) — appended FILTER-DEF ANIMATOR
  driver (Ed/Wrath Designs, Cliche_Beat):** both PSID vectors re-pointed at
  a driver APPENDED after the canon body that ANIMATES THE DATA the player
  reads: init pokes defs 0-2 (r0=$11, init=$02), builds a 256-byte triangle
  table, aims an SMC JSR; each play() the wrapper JSRs the SMC target then
  JMPs the canon play body. Phase 1: every 8 plays defs 0-2 r0 += $10 until
  def0 == $F1, then the stub RETARGETS the SMC JSR to phase 2: every 12
  plays def0.init = tri[i--], def1.init = tri[j], j += 2 (the third store
  hits def1 TWICE — an author bug leaving def2 static; reproduce as-is).
  PRESENTS: D417's res nibble WALKS across note-inits ($17→$37→$47) while
  fbase and the def table's FILE bytes are constant — taint the def r0 byte
  (DYNAMIC, 15 distinct ascending values = the tell it's animated, not
  wedged). The FILE byte at the SMC slot is STALE (ships aimed at phase 2;
  init re-aims to phase 1) — probe the aim from the init immediates, never
  the slot. FIX: `factory._filterdef_anim_probe` (full-shape template match
  anchored from the two vectors, fail-open, lifts step/cap/periods/tri
  seeds) → `filterdef_anim` param → composer `playfda` play-wrapper chunk
  (phase flag replaces the SMC; triangle GENERATED from its seeds; targets
  `fdres+0..2`/`fdinit+0..1`, valid because composer def slots are dense in
  orig order). Masked-compare equivalence: orig CMPs def0's full r0 vs $F1;
  composer's fdres holds r0&$F0, so cap rides as $F0 with the probe
  asserting (cap^r0_init)&$0F == 0. Kin of C18 (it IS a play-vector wrapper
  with phase behaviour) but the phases mutate DATA, not the call schedule;
  sibling of the `filter_mod` cutoff-LFO chunk (Ed/Core_of_Acid). Census:
  signature singleton corpus-wide (zero exposure).
- **19th occurrence (round 108, 2026-07-23) — filter-tail stub sonifies the
  POWER-ON RAM PATTERN into player DATA (Ed/Go_Funk):** the filter tail's
  final `STA $D417` (base+$AC) is re-pointed at a stub that does the store,
  then every 24 plays (first after 11) pokes THREE data cells from
  addresses PAST THE IMAGE END — def 0's init-cutoff from page $EF and two
  WAVEFREQ-table bytes (offsets 14/29, one X2+=2 walk feeding both) from
  page $EE. The environment bytes are libsidplayfp's power-on pattern
  (C29): ⚠ `--peek-post-init` reports RELOCATED-psiddrv bytes at those
  addresses and DISAGREES with the play-run RAM — memwatch is the ground
  truth; `_poweron_fill` models it exactly. A poked wavefreq byte is a NOTE
  OFFSET (pattern $FF = -1 shifts every note that wave step plays). FIX:
  `factory._d417_tail_anim_probe` (full-shape, fail-open) →
  `d417_tail_anim` param → composer `playgfa` chunk at play START with the
  counter seeded +1 (orig pokes at play END; identical observability — the
  cells are only read inside play bodies) + generated power-on pages; the
  wavefreq pokes require the LAYOUT-PRESERVING wave pool, so the extract
  forces `wave_table_pos` for carriers (dropping the param if the layout
  can't be proven verbatim). MIS-ATTRIBUTION TRAP: with the 11-byte
  instrument stride the two secondary targets first read as "unused
  instrument bytes" (unobservable) — mapping them against the WAVEFREQ
  table base showed they were note offsets; check EVERY table's base
  before dismissing a poke target as dead.
- **20th occurrence (round 109, 2026-07-23) — third Ed appended filter-def
  driver (Only_Ones):** two-phase SMC-retargeted animator over the def
  table: phase A (every 16 plays) ramps def2's init/stop toward $62 with
  its res nibble following a 1..15 counter, then walks def1's init/stop
  DOWN to $02, then retargets; phase B (every play) serves def2 init/stop
  = tri[X1]/2+$0A and def1 = tri[X2]/2+$0C from an init-generated 256-byte
  triangle (asc i+4 / desc $83-i), X1 +1 per 8 plays, X2 +1 per 16. FIX:
  `factory._filterdef_anim3_probe` (template-matcher with captured holes +
  operand cross-references — counter slots, SMC slots, def-cell targets all
  verified to point at each other) → `filterdef_anim3` param → composer
  `playooa` chunk (phase flag; fdres holds r0&$F0 so the res write is
  counter<<4). Ed's drivers are each corpus singletons but share the idiom:
  vectors (or one canon store) re-pointed at an appendix; SMC immediates as
  counters/indexes; generated tables; def/wave cells as the mailbox. When
  the next one appears, check THIS entry's three probes first.
- **21st occurrence (round 115, 2026-07-24) — per-play fclaim CLEAR
  re-pointed at a VOID (Gomez/Jezuseczek):** the canon play body opens
  `LDX #$00 / STX $1720` (base+$90); the wedge re-points the STX at $3F20
  (past the image, read by nothing), so the filter CLAIM persists forever
  after the first filter voice sets it — the filter program never steps
  again, and every later cutoff change comes solely from $F1 filterset
  commands (the orig freezes at each command's INIT cutoff; a canon
  rebuild keeps ramping — TELL: $D416 matches for thousands of frames
  while commands drive it, then forks right after a filterset row). The
  route_clear_dead sibling one wedge class over: same "a state CLEAR
  store re-pointed at a void ⇒ the state accumulates/persists" shape,
  different variable. FIX: `factory._fclaim_clear_dead_probe` (anchors
  LDX #$00 + STX, fires only on an out-of-image target — an in-image
  repoint would be a data poke, a different wedge) → `fclaim_clear_dead`
  param → composer drops its per-play `stx fclaim` (fresh var starts 0 =
  the orig's init-cleared state). Corpus singleton (probe census over all
  5401 f1).
- **22nd occurrence (round 117, 2026-07-24) — $FF track-loop handler
  re-pointed at INIT: the song RESTARTS at the first track end
  (Greenhorn/Second + My_Firsty):** canon `A9 00 / 9D 26 17 / 4C D2 10`
  (otrk=0, re-fetch) patched to `A9 00 / 20 <rts> / 4C <init-body>` — a
  byte-preserving neutered JSR where canon has the store, then JMP to the
  member's init. The FIRST voice to hit $FF re-runs init(A=0) mid-frame:
  the init's $D418 + ascending $D400-$D417 clear land MID-STREAM, init's
  RTS pops the voice call so the play body's remaining units + filter tail
  run with fresh state, and the song plays from the top with init-cleared
  state (NOT loop-carried sticky state). TELL: ~99% prefix ending exactly
  at songlength with orig writing $D418 + a full ascending SID clear while
  the rebuild plays on. FIX: `_track_ff_reinit_probe` (anchors the full
  patched shape; JMP target need NOT equal the jump-table init operand —
  Second's JT points at a stub while the wedge jumps the body) →
  `track_ff_reinit` param → the composer's $FF fetch TAIL-CALLS its own
  init (`lda cursong / jmp init` — same RTS-pop mechanism; `cursong` lives
  outside the init-cleared state block). The composer's init SID-write
  sequence ($D418 then the ascending clear) already byte-matches canon's.
  2 carriers HANDLED: Second partial→FULL; My_Firsty was FULL with the wrap
  PAST its verify window (the C32 past-window-latent class — the param makes
  it correct there too), re-verifies FULL.
- **23rd occurrence — SHAPE B: $FF re-points at init WITH a ghost-unit tail
  (Hallen/For_Party_V_95):** a second wedge shape `A9 00 / 4C <tgt>` (JMP
  straight to the target, no neutered JSR) where the wrap voice is NOT the
  last play unit. Two parts, both needed for FULL:
  (1) **the restart** — same RTS-pop mechanism as shape A, but reached via a
  bare JMP;
  (2) **the ghost tail** — init's RTS pops the wrap voice's call, so the play
  body's `inx : jsr voice` chain runs the REMAINING voices with X past the
  3-voice range. On the ORIG memory map those aliased reads/writes emit a
  member-constant SID burst on V1's registers AND poke the SURVIVING (idle)
  voice's state so it plays a real part in the restart instead of idling.
  **CORE-TENET reproduction (NOT the aliasing — a different memory map ⇒
  different ghost writes):** the composer's $FF handler jumps to an
  out-of-line `reinit_ghost` routine that `jsr init`s, emits the captured
  ghost burst VERBATIM, pokes the surviving voice's state to the captured
  values, then `pla:pla:jmp ptail` (drop the wrap voice's return, skip our
  own — wrong — ghost voices, complete the frame at the filter tail). Burst +
  pokes are extract-captured (`_simulate_reinit_ghosts`): burst from the orig
  siddump writelog (the in-window init burst, which ALSO gates in-window);
  pokes from a py65 play-to-wrap RAM diff vs a clean init(A=0), each differing
  per-voice slot mapped to its composer label.
  **THE DISCRIMINATOR IS THE JMP TARGET, not just in-window.** The 11 "shape B"
  siblings (Hank/*) that made the first pass regress 12/14 do NOT re-init at
  all: their `A9 00 / 4C` jumps to the re-fetch LOOP code ($101E `LDY otrk,x`),
  not init — they are loop-redirects the ordinary path already handles. So the
  probe requires the JMP target to LEAD TO INIT (== the init vector `base`, or
  the init body `_rd16(base+1)`); a target that lands in the loop code is
  rejected. This target check + the in-window burst gate are BOTH satisfied
  only by the true re-init carrier ⇒ the changed verdict has no false positive
  (every other member gets no param ⇒ byte-identical). The first pass's
  regression was loose `A9 00 / 4C`-only detection (catching the loop-redirects)
  + a compile bug — NOT a fundamental conflict.
  **THE FREQ-DETERMINING POKE IS BELOW $1718.** init clears only $1718-$179D;
  the surviving voice's `curnote`/`gatemask` live at $1012/$100F (uncleared),
  so the ghost's curnote poke there is what makes the voice play its real note
  — the below-$1718 slot the first pass skipped (it only scanned $1718-$179D
  and stalled at 98.4%). The RAM-diff capture spans $1000-$17FF, so it is
  found automatically.
- **Status:** CANONICALIZED (23 occurrences: DMC family-1 rounds
  13 + 19 + 32 + 35 + 36 + 50 + 55 + 60 + 63 + 65 + 70 + 95 + 96 + 101 + 102 + 106 + 108 + 109 + 115 + 117 + 118). Canonical form: STATIC opcode probe (read the patched
  instruction itself — never a bounded write-stream scan, which can
  false-negative on members that exercise the path only late) → factory
  `extra_params` → an existing/new composer param; census carriers on BOTH
  sides (partials = recovery set, FULLs = exposure set to re-verify+rewrite).
- **2nd occurrence (round 19):** the holding gate-off 1-BYTE patch —
  sub_17EC's `$17EF: BC->60` (LDY→RTS) turns `gate mask + AD/SR=$00` into
  mask-only. A widespread editor build (Surgeon/Imaic/Rio/Taxim/Phobos/
  Behdad_Arman: 514 FULL + 97 partial carriers); 179 of the FULLs had been
  rescued only by the batch's blind `frames_clear_adsr` mask_only retry,
  and the 97 partials were exactly the members that retry could NOT reach
  (their origs write AD/SR=$00 through OTHER paths, so the stream scan
  gates off). `factory._hold_gateoff_probe` follows the holding-branch JSR
  by opcode shape (layout-blind, C13 corollary) and classifies the byte
  after the gate-mask STA. The param (`hold_gateoff='mask_only'`) already
  existed for family-2 — the probe just feeds it for family-1 carriers.
- **4th occurrence (round 35):** the dual-effect FREQ-GENERATOR wedge
  (Taurus/Taurus_02, the only carrier in ALL 10,676 DMC members): the dual
  ($40) odd-parity path's `LDA $172F,x` opcode patched BD→A6 (`LDX $2F`;
  zp $2F = $A9 under the PSID environment) so every subsequent per-voice
  read lands +$A9 past the state arrays — onto fixed CODE bytes (slide
  speed = a JMP opcode, base hi = a CMP operand, PW/ctrl = sub_17EC/17FB
  bytes) — while the "slide accumulator" self-modifies two tune-setup code
  bytes (file-image values = the seed) and the update ORs in a BASIC ROM
  byte ($BD68,y) and rotates a feedback byte via an ILLEGAL RRA on zp $12.
  Net: one GLOBAL free-running pseudo-random freq ramp on dual frames +
  pwphase[V3] clobbered to $42/$43 (live-carry-dependent), driving the
  pulse machine's speed fetch off the instrument record (static bytes past
  the table). Fix: `factory._dual_freq_gen_probe` (wedge-anchored regex, all
  effective constants captured from the image) → `dual_freq_generator` param
  (renamed from `dual_hack` 2026-07-06, C7-note decision) →
  composer emits the generator as CLEAN code (legal ror+adc = RRA,
  inlined constants, live-carry pwphase store) + `dual_generator_steps`
  (extract-captured static bytes) EXTENDS the stride-8 isteps/irawsp
  tables at the reachable garbage-phase indices — zero pulse-code change.
  Python-simulate the generator against ALL observed dual events (3826/
  3826) BEFORE composing. Default byte-identical (3-member MD5).
- **5th occurrence (round 36, 2026-07-06):** the hard-restart AD/SR
  IMMEDIATE patch (Stryyker, 4 carriers, all family-1, all value $0A):
  sub_17FB's `LDA #$0F` operand ($17FF) patched to $0A, so every
  note-fetch frame primes AD=SR=$0A instead of $0F/$0F. The simplest C19
  form yet — one immediate byte. `factory._hr_preset_probe` anchors on the
  routine's opcode shape (`[99|B9] 04 D4 A9 vv 99 05 D4 99 06 D4 60`,
  layout-blind; first opcode admits $B9 for the _hardrestart_smc_variant_probe SMC
  variant) and feeds the value through the EXISTING `hard_restart` param
  (domain extended: 'preset'/'none'/numeric — no new schema field);
  composer renders `lda #$vv`. Guarded against family-2's preset
  'none'. Default renders identical text → byte-identical. Census over
  all 10,676: 4 carriers, 0 FULL exposure; 3 flipped FULL, Sans_intro has
  an unrelated pre-existing first blocker (identical flat_div before/after).
- **3rd occurrence (round 32):** the PW-hi SOURCE patch (Olsen/Lame, 1
  family-1 carrier): the sidwrite tail's `LDA $1753,x / STA $D403,y`
  operand re-pointed at base+$707 (the track-ptr lo triple, constant after
  init) — each voice's audible PW hi pinned at a per-voice constant while
  the internal PWM machine still runs on $1753. `factory._pulsewidth_hi_const_probe`
  anchors on the `$D402/$D403` store pair + the canon PW-accum-lo operand,
  captures the POST-INIT bytes at the patched operand → `pulsewidth_hi_const='a,b,c'`
  param; composer swaps the pwwrite source to a 3-byte table. Default
  byte-identical; base-relative census proved exactly 1 carrier.
- **COROLLARY — a probed knob must be honored on EVERY orig path that funnels
  through the patched site (2026-07-06, Chojnow_Music_Compo_1).** A wedge is
  ONE instruction, so every orig code path reaching it inherits the variant
  behaviour; the composer's re-architected handlers must EACH route through
  the parametrized target. The `rest_effects='skip'` JMP ($117D → $1591) is
  shared by rest, switch, slide AND the $7C soft-note fetch — composer's
  `ev_n_softq` hard-coded `jmp run_effects`, so soft-note fetch frames stepped
  the pulse where the orig held it (V2 PW one step ahead from the first soft
  fetch, flat div 266023). Fix = `jmp {rest_jmp}` (byte-identical for canon
  'run'). When landing a knob, grep the composer for ALL jumps to the
  canon-target label and check each against the orig's funnel paths.
- **6th occurrence (round 50, 2026-07-08):** the PWM bound-A SHIFT wedge
  (Aomeba/20_Years_of_NOP, the ONLY carrier in all 5401 family-1 members):
  note-init byte $124D patched $4A->$17 (LSR -> the 2-byte illegal SLO $4A,X,
  which ASLs an UNUSED zp scratch byte + ORs 0 into A = inert; zp $4A-$4C are
  unreferenced by the player), so the bound-A extraction runs LSR x2 not x4:
  bound A = byte+2 >> 2 (not its hi nibble), bound B = A EOR $0F. Effect: the
  PWM sweep hi-byte bounces over a much WIDER band before flipping (inst
  byte+2 $77 -> canon bounds 7/8, wedge bounds $1D/$12; the rebuild flipped at
  pwh=8 where the orig ramps 7->8->9->10..., first div V2 PW frame 4). CLEANEST
  C19 outcome yet — EXTRACT-ONLY: the bound values ARE musical content (USF
  min_hi/max_hi), so the probe only fixes their DERIVATION; no USF field, no
  composer change. `factory._pw_bound_shift_probe` anchors on the
  STA $1756,x / EOR #$0F / STA $1759,x tail (reloc-aware, operands
  base+$756/$759), decodes the 4-byte PLA->STA window counting LSR-A ($4A;
  $17 = known 2-byte filler, any unknown opcode bails to canon). Threaded as
  EXTRACT-ONLY `cfg.extra_params['pw_bound_shift']` (POPPED before the USF
  params block so the derivation knob never leaks to ML — the derived bounds
  already carry the music). Census: 1 carrier, 5400 canonical (shift=4 =
  byte-identical) => regression-safe by construction. 20_Years_of_NOP
  partial -> FULL 294517/294517.
- **7th occurrence (round 55, 2026-07-08):** the hard-restart prep-CALL SKIP
  wedge (SilverFox/Seaside_99 + 8 more, 9 family-1 carriers). The note-load's
  hard-restart primer `LDA #$08 / JSR sub_17FB / LDA #$FF` (base+$1D9..$1DF;
  sub_17FB writes TEST $08 + AD/SR $0F0F on the fetch frame) has its JSR opcode
  patched $20->$2C = `BIT $17FB`, neutering the ENTIRE call: the fetch frame
  writes NOTHING (no TEST, no AD/SR), while pending (base+$4A) is still set so
  the note inits normally next frame and the old note rings through the fetch
  frame. Presents as the rebuild emitting an EXTRA 3-write prep block
  (D40x=08/0F/0F) at each note-fetch frame that the orig lacks — localize
  per-IRQ (Trap-C-free) so the extra writes don't smear across frame buckets;
  the pc-trace ($11DB = `2c fb 17` not `20 fb 17`) is the ground truth (the
  memwatch showed pending going FF = hard-restart path taken, which
  contradicted "no prep" until the pc-trace revealed the neutered opcode).
  Distinct from 'none' (family-2, keeps the $08 TEST write) and the numeric
  preset wedge (5th occ, patches sub_17FB's immediate, call intact). Fix:
  `factory._hr_prep_skip_probe` (STATIC opcode probe, reloc-aware base+offset,
  verifies the shape both sides: LDA #$08, the sub_17FB operand = base+$7FB,
  LDA #$FF) → the EXISTING `hard_restart` param, domain extended to a 4th value
  'skip'; composer suppresses BOTH `hr_test_write` and `hard_restart_adsr` in
  `ev_n_hard`. NB some carriers ALSO patch sub_17FB's first byte $99->$60 (RTS)
  — irrelevant since the call is neutered, so the census keys on the call-site
  opcode + the reloc-invariant `op - code_start == $622`, never on sub_17FB's
  shape. Census over all 5401 f1: exactly 9 carriers (Welcome_to_Egypt, Bayliss
  DMC_Collection_3_intro / DMC_V4_0_Collection_note / Tarkus_4K /
  Snowball_Caper_2, DaFunk I_Dont_Need_Love / 3-Speed, SilverFox Poison_Girl /
  Seaside_99), ALL partial (0 FULL exposure) => regression-safe by
  construction; ALL 9 partial -> FULL. 0 f2 carriers.
- **24th occurrence (round 121, 2026-07-27):** the $7D-RETRIG dispatch-branch
  wedge (Heinmueck/Dreck_Ist_Weg, sole HVSC carrier of 10,689 scanned). ONE
  BYTE: the SWITCH dispatch `CMP #$7D / BEQ` operand at base+$12C patched
  $56→$2B — the branch target moves from canon's switch-toggle handler
  (base+$183) to canon's OWN mode-0 glide replay tail (base+$158: `LDA
  base+$744,x / JMP base+$1A6`). Semantics: $7D = FULL NOTE-INIT of the
  stored glide-start note (the register only glide rows write; init clears it
  to 0), transpose add skipped (enters at base+$1A6), switch flag untouched.
  Presented as a missing V2 note re-trigger at frame 153 (orig $08/$0F/$0F
  prep + fresh drum init; rebuild released instead — our decode read $7D as
  canon SWITCH → gate_toggle row). Two detection lessons: (a)
  `dmc_canon_diff` MISSED it — it reported only the $10DF track-loop hook; a
  same-opcode BRANCH-OPERAND repoint within the player body is outside its
  cluster classes, so a "no NEW wedges" report does not cover branch
  operands — the raw byte-diff vs the canon binary (774 diffs → 2 real code
  wedges after operand/data filtering) found it in one pass; (b) the member
  fell through to the DATAFLOW path (canon compare rejected $112C), so the
  cure re-homes it: `factory._switch_retrig_probe` validates the exact
  target+tail, MASKS the one operand byte, and the carrier flows through the
  canonical build (gaining every canon wedge probe). Fix is EXTRACT-ONLY
  (typed `cfg.switch_retrig`, never USF): the walk keeps a per-voice shadow
  of base+$744,x ('abs', transpose folded in at store time) + curnote
  ('cur'), and decodes $7D as a plain note row `(abs - transpose) & $FF`
  (playback's re-add reproduces the index; ≥$60 = refusal). The stream is
  byte-identical to a normal note row from base+$1A6 on, so the composer
  needs nothing. Shadow state joins the walk's endless/wrap keys only when
  the wedge is on. Dreck partial 2.8% → FULL 85879/85879.
- **25th occurrence (round 122, 2026-07-27):** the DRUM FREQ-HI REPOINT wedge
  (Heinmueck/Enforcer_2_Level_1_preview, sole carrier — one of the canon_diff
  census's 9 known unhandled singletons). The absolute-freq wave step's hi
  store (canon $15FD `STA $1732,x` = fbh) is re-pointed at base+$754,x =
  pwh+1, the NEXT voice's pulse-width-hi running state. Effect: a drum step
  zeroes fbl but LEAVES the voice's SID freq hi at the note's base value
  (orig wrote the stale note hi $1A where canon semantics write the wave
  table's $0C — the divergence signature: wrong VALUE on freq hi at a
  drum-ish note event, `dmc_offtable_probe` correctly reports NOT an
  off-table read), while the wave table's freq byte pokes the next voice's
  PW hi. The same member also NOPs out the V3 play-unit call ($109C/$109D
  INX+JSR → 4×NOP — the C24 unit-removed form, already handled as
  `play_unit_repeat='1,1,0,1'`) and carries the $10DF track-loop hook.
  Fix: uniform `_WEDGE_PROBES` row `factory._drum_fhi_probe` (anchored on
  the canon `LDA #$00 / STA fbl,x / LDA abs,y` shape, fires only on the
  exact base+$754 operand) → `drum_fhi_to_pw` param → composer_asm's
  `ws_drd` hi store becomes `sta pwh+1,x` (X=2 unreachable — that carrier's
  V3 unit is removed; a future V3-drum carrier would hit whatever follows
  pwh in OUR layout, verify judges). No canon-path masking needed: the
  member builds via the DATAFLOW path, which already runs the uniform wedge
  probes. Enforcer_2 partial 27% → FULL 57236/57236.
- **27th occurrence (2026-07-29, r134, Road_to_Sydney +1 FULL, sole carrier)
  — the $FF handler REPLACED by a `JMP` to an appended PER-VOICE-immediate
  stub.** A fourth $FF-loop form beside canon / immediate-patch (15th) /
  JSR-hook: `C9 FF D0 08` then `4C stub` (the whole `A9 00 9D otrk` gone);
  the stub is `CPX #$02 / BEQ / LDA #aa / JMP / LDA #bb / STA otrk,X / JMP
  dispatch` — V1/V2 loop to track position aa, V3 to bb, IGNORING the
  data's loop byte. With no canon loop site, dataflow's binary
  classification defaulted to `track_loop_target=True` (read-next-byte),
  decoding GARBAGE loop targets — the C13 "a binary not-A⟹B classifier
  hides a THIRD form" lesson recurring at the $FF site. PRESENTS: a
  mid-song row-duration divergence (ours ends a row early after the wrap)
  — the wrap itself is silent because both sides re-enter the SAME pattern,
  diverging only when the post-wrap entry SEQUENCES drift apart. Fix:
  positive stub-shape recognition in `dataflow.locate` → the existing
  per-voice `loop_reset_pos` tuple (round-63 machinery, no new plumbing).
- **28th occurrence (2026-07-29, r136, Party_Pooper_3_intro +1 FULL, sole
  carrier) — the NEGATIVE-TRANSPOSE ADC immediate.** The track transpose
  handler's $80-$9F branch is canon `EOR #$1F / ADC #$01` (two's-complement
  negative: $81 → −1); this member patches the immediate to #$11, biasing
  every "negative" transpose by +$10 ($81 → +$0F). PRESENTS: a deep
  note-init pitch divergence (~+16 semitones) at the first orderlist entry
  whose transpose command falls in $80-$9F — the walk decoded canon −1
  while the engine's live transp read $0F. EXTRACT-only per the C19 rule
  (the wedge changes a DERIVED musical value): `cfg.transpose_neg_bias`
  (canon 1, probed off the `49 1F 69 imm 9D base+$72C` shape) → the walk's
  negative-branch arithmetic. The probe emulates the exact ADC chain, not
  an algebraic model (C11's refinement).
- **26th occurrence (2026-07-29, r133, Grapevine_18_intro +1 FULL) — the
  cymbal noise-burst CTRL immediate ($81 → $02, sync-only attack).** The
  immediate-value tweak class `dmc_canon_diff` documents as its blind spot,
  and the SECOND immediate of a burst whose FIRST was already probed:
  `_cymbal_burst_byte`'s pattern HARDCODED `A9 81` for the ctrl, so it could
  match only freq-patched members — a probe that anchors on one canon
  immediate is blind to a patch of that same immediate. Generalized to
  capture (burst, ctrl); `cymbal_ctrl` param, default $81. LESSON: when a
  multi-immediate site gets a probe, capture EVERY immediate — the anchored
  ones are exactly the ones a wedge can also hit. (Diagnosis detour worth
  the note: the member also re-assembles its instr cache one byte up
  ($1016-$1018), so canon-address memwatch showed the WRONG voice's
  instrument and mimicked a cross-voice wedge — check the member's own
  LDA/STA operands before believing a canon-address watch.)
- **Consumers:** DMC v4 `factory._hardrestart_smc_variant_probe` + composer_asm
  hardrestart_smc_variant/hardrestart_test_init gating; `factory._hold_gateoff_probe` →
  `hold_gateoff` param; `factory._pulsewidth_hi_const_probe` → `pulsewidth_hi_const` param;
  `factory._hr_preset_probe` (numeric) + `factory._hr_prep_skip_probe`
  ('skip') → the shared `hard_restart` param (domain
  preset/none/numeric/skip). Sibling of C18 (wrapper OUTSIDE the player) —
  C19 is patches INSIDE the canon body. The round-14 $D418 play-vector prefix
  (`LDA #imm/STA $D418/JMP base+3`, factory `_d418_play_wrapper` →
  `master_vol_every_play`, +6 FULL, commit efbf639) is the degenerate stateless
  case: a wrapper with NO phase behaviour — probe the PSID play vector's
  target shape whenever a member's play ≠ base+3 OR the JT entry target ≠ the
  canon play body.
  **32nd occurrence — INDIRECT topology (round 144, 2026-07-30, PVCF/Scratch_It
  FULL 303880):** the wrapper need not be INLINE at the PSID play vector. The
  canon play JT slot itself (base+3) can be re-pointed `JMP <appended wrapper>`,
  and the wrapper does `LDA #imm / STA $D418 / JMP <real play body>` (Scratch_It:
  play $7003 = JMP $82F0, wrapper $82F0 = `LDA #$1F / STA $D418 / JMP $7085`).
  The old probe read the shape INLINE at `s['play']` (exit == base+3) and so
  missed this — exactly the "JT entry target ≠ canon play body" case flagged
  above. `_d418_play_wrapper` now also follows ONE JMP from the play vector and
  anchors on the reloc-invariant `A9 ?? 8D 18 D4 4C` shape with an in-image,
  non-self-loop exit target — strict enough that normal play bodies
  (`DEC $xxxx…` / `LDX #0…`) and CIA-latch wrappers (`STA $DC05`) do NOT
  false-fire. Composer side is unchanged (its `playd418` chunk already emits the
  indirect form). Census: exactly 2 indirect carriers in 10,676 — Scratch_It +
  Bayliss/Follow_That_Storm; the latter is a re-assembled NON-canon-geometry
  member (init b+$50, play body b+$7B) that fails EARLIER at jt-detection, a
  separate lever. Occurrences in [[project_dmc]].
- **A custom driver that RE-SHAPES a MUSICAL VALUE is DECONSTRUCTED to content
  (EXTRACT-only), NEVER reproduced in the composer (2026-07-31, Rayden f1 +3:
  Mr_Siegfrieds / Revel_99 / Roing_Rown — the 33rd wedge, but the FIRST logged
  as extract-only-deconstruction).** Rayden's DMC build APPENDS a non-canon
  duration-ramp driver: an SMC index cycles a 4-entry table (`$101C=[5,4,3,2]`)
  and writes it to ALL three voices' durrel (`$173E-$1740`) on each V1
  note-advance, so the note duration is a GLOBAL period-4 beat (canon durrel is
  PER-VOICE, set by an `$80-$BF` command). All voices advance in LOCKSTEP (one
  row per beat). A member whose sector carries no `$8x` (so every note relied on
  the ramp) decoded as dur=0 → held 256-tick notes → the voice never advances.
  STATIC PROBE (`_durrel_ramp_probe`): three consecutive `STA durrel_v,abs`
  (canon writes `STA $173E,X`, never three absolutes) + the `LDA table,X`
  operand = the ramp table.
  ⚠ **THE FIRST INSTINCT IS A PRINCIPLE §8 LEAK.** A composer param that
  reproduces the ramp routine is "a Rayden-specific 6502 emitter path selected
  by USF content" — the §8 forbidden shape wearing a C19 costume. The CORE TENET
  ("reproduce the write stream, not the code — SMC ESPECIALLY") and THIS entry's
  own boundary ("EXTRACT-only when the wedge changes a derived musical VALUE;
  COMPOSER param when it changes a write-stream TIMING/VALUE the composer can't
  otherwise emit") both route a DURATION wedge to EXTRACT-ONLY. Note durations
  are CONTENT; the ramp is space-saving MECHANISM (Principle Rule 1).
  FIX (extract-only, NO schema, NO composer change): stamp each row's `duration`
  (the EXISTING field) = table[i % 4]. Every pattern is 4-BEAT-ALIGNED
  (row count % 4 == 0) — so pattern-row i is ALWAYS at global beat-phase i % 4
  regardless of orderlist position → NO drift, NO C32 variants, durations are
  pattern-intrinsic. The engine-blind composer plays them via its ordinary
  duration path (it already emits durations for every `$8x`-carrying member).
  GATED to rows with dur==0 (no `$8x`): an `$8x`-driven carrier keeps its stated
  durations → BYTE-IDENTICAL. CENSUS: 6 Rayden carriers — 3 partial→FULL (all
  no-`$8x`), 2 `$8x`-driven stay FULL byte-identical (Rock_Remake /
  Sealed_Universe), 1 residue (Embarassed_Emotions, nonstandard play=$0000
  vectors, unrelated). Non-carriers probe None. The 4-alignment is what makes it
  clean — a non-aligned ramp member would need the global beat index and is left
  as honest residue (none exist in HVSC). Diagnosis TELL: a voice whose note
  NEVER advances (held-forever) + every decoded row dur=0 + a non-canon writer
  of durrel (the canon `$17E0` never fires under pc-watch). Occurrences in
  [[project_dmc]].
- **34th occurrence — POST-NOTE GUARD immediate patched (2026-07-31, Rayden
  f1 +1, sole carrier).** The note-init sets the post-note guard with `LDA #$02
  / STA $1786,x` at canon `$12F8`; while the guard is >0 it is DEC'd each frame
  and the end-of-note gate-off logic (`L_132D`) is SKIPPED, so a fresh note
  stays gated for 3 frames minimum. A wedge patches the immediate `$02 -> $00`
  → the guard starts at 0 → gate-off runs on the FIRST frame after note-init →
  the note's ctrl steps to `wavectrl & $FE` (gate cleared) one frame in instead
  of three. This changes the ctrl WRITE-STREAM (the gate bit over the note's
  frames), so it is a COMPOSER param, not extract-only (contrast the 33rd:
  durrel is a musical VALUE → extract; this is a write-stream gate TIMING the
  composer must emit). Min gate-on = imm+1 frames — a per-note articulation.
  TELL: the first divergence is a voice ctrl `$20` (orig, gate off) vs `$21`
  (rebuild, gate on) on the first wave-step after a note-init, with the wave
  table cell = `$21` (the gate bit is being masked off by the gate logic, not
  stored). Confirm by memwatch of the gate mask (`$100F,x`) + guard (`$1786,x`,
  NB `,x` so V3 = `$1788`) — orig guard is 0 at note-init where canon is 2. FIX:
  `factory._note_guard_probe` (STATIC opcode probe, reloc-aware — anchors LDA#
  opcode + STA-abs,x opcode + operand == base+$786, returns the immediate iff
  != $02, else None) → composer `note_guard_init` param → the note-init emits
  `lda #$<imm>` instead of the hardcoded `#$02`. Regression-safe by
  construction: no param → `lda #$02` textually unchanged → byte-identical.
  CENSUS over 5833 f1: exactly 1 carrier (NOFX_tune_2, guard=0, was partial),
  0 FULL carriers ⇒ 0 exposure. Occurrences in [[project_dmc]].
- **35th occurrence — PULSE UP-REVERSAL bound operand repointed (2026-07-31,
  Rygar/Complications +1).** The canon pulse up-sweep at `$1381..` adds the
  step to the PW accumulator then reverses when pwh == bound B: `... STA
  $1753,x / CMP $1759,x` at canon `$1393`. A wedge re-points the `CMP` operand
  `$1759` (bound B, `$175B` for V3) → `$1710` (the per-voice filter route-bit
  CONST `$01/$02/$04`). So the up-sweep reverses when pwh == the route bit
  instead of at bound B: a voice whose PW starts ABOVE its route bit (V3 pw
  starts pwh=$08 > route bit $04) never hits it, so the PW ramps the FULL
  16-bit range (wraps at $FFFF) instead of oscillating in the small
  bound-A..bound-B window — a deliberately wide PWM. Changes the PW WRITE
  STREAM (the pwl/pwh values over the whole note), so a COMPOSER param. FIX:
  `factory._pw_up_reverse_probe` (STATIC reloc-aware opcode probe — anchors the
  pwh LDA/STA operands == base+$753 + the CMP opcode, returns 'routebit' iff
  the CMP operand is base+$710) → composer `pw_up_reverse='routebit'` → the
  shared `pw_sweep` block emits `cmp fbit,x` (the composer's `$01/$02/$04`
  route-bit table) instead of `cmp cpwmax,x`. Regression-safe by construction:
  no param → `cmp cpwmax,x` textually unchanged → byte-identical. TELL: a deep
  PW divergence where the ORIG sweeps monotonically past its bound-B (never
  reversing up) while the rebuild reverses — memwatch shows bound B is correct
  but the orig ignores it; byte-diff the orig's `$1393` CMP operand vs canon
  `$1759`. CENSUS over 5833 f1: 2 carriers, both repoint to $710 — Complications
  (was partial → FULL) + Control/Hexen_Remake (FULL, stays FULL: emitting the
  faithful reversal only matches the orig it already used). Occurrences in
  [[project_dmc]].
- **36th occurrence — STATIC $D418 (init-wrapper primes it once, play writes
  NOPed) (2026-07-31, Signor/Logic_Intro +1, sole carrier).** The canon engine
  writes $D418 twice: at init (master vol, `STA $D418` at base+$5C) and at every
  filter note-init (mode|vol, `STA $D418` at base+$2A8). This member NOPs BOTH
  (`EA EA EA`) and an appended PSID-init WRAPPER (`JSR base / LDA #$3F /
  STA $D418 / RTS`) writes $D418 = a fixed mode|vol immediate ONCE — so $D418 is
  set once at init and NEVER touched during play (a STATIC filter mode+vol for
  the whole tune). The default composer instead writes $D418 = mode|vol at every
  filter note-init (Logic_Intro: 501 spurious writes vs the orig's 2). TELL: the
  rebuild emits a $D418=mode|vol at a filter note-init where the orig has none,
  and the orig's TOTAL $D418 write count is ~2 (init only) vs the rebuild's
  hundreds — count $D418 writes across the song. FIX: `factory.
  _master_vol_static_probe` (anchors BOTH canon-$D418-store NOPs at base+$5C and
  base+$2A8, then reads the immediate from the sole remaining `LDA #imm /
  STA $D418` in the image) → composer `master_vol_static=$3F` → the init primes
  `LDA #$3F / STA $D418` (instead of `STA $D418`=master vol) and the filter
  note-init emits NO $D418 write. Regression-safe by construction: no param →
  the canon init $D418=master-vol write + per-note-init $D418, byte-identical.
  Trichotomy note: a $D418 set once at init and never changed IS a §4.2 priming
  write; modeled here as a C19 sibling of `master_vol_every_play` /
  `master_vol_reassert_filter_tail` for consistency with the DMC composer's
  existing $D418 params. CENSUS over 5833 f1: 1 carrier (Logic_Intro, was
  partial), 0 FULL ⇒ 0 exposure. Occurrences in [[project_dmc]].
- **37th occurrence — STATIC FILTER (play-tail $D416/$D417 stores NOPed)
  (2026-07-31, SilverFox/Blood_2_game +1, sole carrier).** A RE-ASSEMBLED play
  routine (custom $1000-$10BF; the $1100+ note/effect code is canon + table
  relocations) keeps the canon filter tail's LOADS but NOPs the two STORES:
  `LDA $171C / STA $D416 / LDA $1018 / ORA $1723 / STA $D417` becomes `LDA $171C
  / EA EA EA / LDA $1018 / ORA $1723 / EA EA EA`. So the filter cutoff/res are
  set once at init (the $D400-$D417 clear) and NEVER written during play — a
  static filter (the internal cutoff sweep still runs, its result just isn't
  emitted). The default composer writes $D416/$D417 EVERY frame (Blood_2: 2761
  writes vs the orig's 3 = init only). TELL: the orig writes $D416/$D417 only
  ~2-3× TOTAL (init) while the rebuild writes them per-frame — count filter
  writes; the first divergence is a spurious rebuild $D416 where the orig has a
  note write. FIX: `factory._filter_static_probe` (anchors the whole 15-byte
  reloc-invariant NOPed-tail shape — cutoff base+$71C, shadow base+$18, res
  base+$723, both store slots EA EA EA) → composer `filter_static` → the play
  body emits no filter tail. Regression-safe by construction: no param → the
  canon per-frame $D416/$D417 tail, byte-identical. Note the play is
  re-assembled (fixed-offset probes on the $10xx dispatch would miss); this
  probe scans for the pattern anywhere, base-validated. CENSUS over 5833 f1: 1
  carrier (Blood_2_game, was partial), 0 FULL ⇒ 0 exposure. Occurrences in
  [[project_dmc]].
